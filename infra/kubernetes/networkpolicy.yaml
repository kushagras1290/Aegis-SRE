apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata: {name: aegis-api-default-deny}
spec:
  podSelector: {matchLabels: {app: aegis-api}}
  policyTypes: [Ingress, Egress]
  ingress:
    - from: [{namespaceSelector: {}}]
      ports: [{protocol: TCP, port: 8080}]
